Add your real LSO GIF files here (e.g. `hola.gif`, `gracias.gif`, `por_favor.gif`, `ayuda.gif`), then declare each file under `flutter: assets:` in `pubspec.yaml`. Until then, the app shows a tiny transparent placeholder when an asset is missing.

Optional: run `dart run tool/create_placeholder_assets.dart` from the project root to generate minimal valid GIF files with the expected names.
